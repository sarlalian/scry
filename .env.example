# Scry Environment Variables
# Copy this file to .env and fill in your values

# Required: Jira API token
# Generate at: https://id.atlassian.com/manage-profile/security/api-tokens
SCRY_API_TOKEN=your-api-token-here

# Optional: Path to config file (overrides default search paths)
# Useful for switching between work/test Jira instances
# SCRY_CONFIG_FILE=~/.config/scry/test-config.yml

# Optional: Override config file values via environment
# SCRY_SERVER=https://your-domain.atlassian.net
# SCRY_LOGIN=your-email@example.com
# SCRY_PROJECT=PROJ
# SCRY_AUTH_TYPE=basic
